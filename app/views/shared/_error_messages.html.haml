-if registro.errors.any?
  #error_explanation
    %h2= "#{pluralize(registro.errors.count, "error")} prohibited this record from being saved:"
    %ul
      - registro.errors.full_messages.each do |msg|
        %li= msg
